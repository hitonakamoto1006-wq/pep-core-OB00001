{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": ".*",
                "cmd": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"git add .; git diff --cached --quiet; if ($LASTEXITCODE -ne 0) { git commit -m 'Update PEP Core'; git push origin main }\""
            }
        ]
    }
}